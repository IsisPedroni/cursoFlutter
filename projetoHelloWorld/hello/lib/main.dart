import 'package:flutter/material.dart';

void main() {
  // runApp(const MyApp());
  runApp(
    MaterialApp(
      home: Center(
        child: Text(
          'Hello Word',
          style: TextStyle(fontSize: 16, color: Colors.amber),
        ),
      ),
    ),
  );
}
